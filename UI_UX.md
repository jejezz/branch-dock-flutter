# Branch Dock — UI/UX 계획서 (v0.1 초안)

기능 범위는 [PLAN.md](PLAN.md)에 있다. 색, 모서리, 간격, 글꼴, 테마, 언어,
공통 단축키, 상태 화면은
[conventions `ui-ux.md`](https://github.com/jejezz/application-release-templates/blob/main/conventions/ui-ux.md)를
따르고, 이 문서에는 **규약과 다른 점과 이 앱에만 있는 것**만 적는다
(`ui-ux.md` §8).

## 1. 설계 원칙

- **편집기 옆 세로 창**: 화면 오른쪽(또는 왼쪽)에 좁고 긴 창으로 띄우고
  편집기와 나란히 쓴다. 모든 화면은 폭 380px에서 먼저 설계하고, 넓어지면
  여백만 늘린다.
- **위는 상태, 아래는 작업**: 위쪽에는 "지금 어떤 상태인가"(브랜치, ↑↓, 변경
  수)를 항상 고정해서 보여 주고, 아래로 내려가며 작업 목록이 온다.
- **명령이 보이는 UI**: 양식마다 "실행될 명령"을 보여 주고, 화면 맨 아래에
  마지막 명령과 결과를 항상 한 줄로 보여 준다.
- **한 번에 하나의 추천 행동**: 상태를 보고 지금 할 만한 일 하나를 배너로
  제안한다. 여러 개를 늘어놓지 않는다.

## 2. 창 (규약 §5와 다른 점)

| 항목 | 규약 기본 | 이 앱 | 이유 |
|---|---|---|---|
| 기본 크기 | 1200×720 | **440×960** | 편집기 옆에 세로로 둔다 |
| 최소 크기 | ≤ 960×600 | **380×560** | 1366×768 노트북에서 편집기와 나란히 놓을 수 있게 |
| 크기·위치 기억 | 선택 | **필수** | 사용자가 편집기 옆에 맞춰 둔 배치를 유지 |

- 화면 높이가 기본 크기보다 작으면 작업 영역 높이에 맞춘다.
- `P1` **항상 위에 표시** 토글 (앱 바 핀 버튼, ⌥⌘T).
- `P1` **화면 가장자리에 붙이기**: 메뉴에서 "오른쪽에 붙이기 / 왼쪽에
  붙이기" — 현재 화면 작업 영역의 높이 전체, 폭 440으로 이동한다.
- 폭에 따른 배치:

  | 폭 | 배치 |
  |---|---|
  | 380–439 | 섹션 탭을 가로로 스크롤 |
  | 440–839 | 섹션 탭 8개를 같은 폭으로 (기본) |
  | 840 이상 | `P2` 왼쪽에 섹션 레일(아이콘 + 이름), 오른쪽에 내용. 상태 헤더와 배너는 위에 그대로. 창이 낮으면 레일만 세로 스크롤 |

## 3. 화면 구조

```
┌──────────────────────────────────────┐
│ ▣ my-app ▾            ⟳  📌 ◐ 文 ⓘ  │ A. 앱 바
├──────────────────────────────────────┤
│ ⎇ feature/login  → origin/feature/.. │
│ ↑2  ↓0   ● 변경 3                    │ B. 상태 헤더 (고정)
│ [ Fetch ]  [ Pull ]  [ Push ↑2 ]     │
│━━━━━━━━━━━━━━━━━━━ (진행 표시줄)     │
├──────────────────────────────────────┤
│ 💡 커밋 2개가 아직 GitHub에 없습니다 │ C. 추천 행동 배너 (조건부)
│                         [ Push ] ✕  │
├──────────────────────────────────────┤
│ 변경 브랜치 태그 원격 릴리스 PR  CI │ D. 섹션 탭
├──────────────────────────────────────┤
│                                      │
│  (섹션 내용 — 세로 스크롤)           │ E. 내용
│                                      │
├──────────────────────────────────────┤
│ ✓ git push origin feature/login  0.8s│ F. 명령 바 (⌘J로 펼침)
└──────────────────────────────────────┘
```

### A. 앱 바 (높이 44)

- 왼쪽: **저장소 전환** 버튼 — 저장소 이름, 누르면 최근 저장소 목록과
  "다른 폴더 열기…"(⌘O). 툴팁에 전체 경로.
- 오른쪽: 새로 고침(⌘R), 항상 위(핀), 그리고 규약의 `테마 | 언어 | 정보`.
  아이콘은 툴바 크기 18.

### B. 상태 헤더 (항상 고정)

- 1행: 브랜치 칩(누르면 브랜치 빠른 전환 목록) → 추적 브랜치(없으면
  "원격에 없음" 회색 pill).
- 2행: `StatusPill`로 상태를 보여 준다.

  | 상태 | 표시 | 색 |
  |---|---|---|
  | 원격보다 앞섬 | `↑2` | primary |
  | 원격보다 뒤처짐 | `↓3` | warning |
  | 변경 있음 | `● 변경 3` | textMid |
  | 깨끗함 | `✓ 최신` | success |
  | 병합 중 / rebase 중 | `병합 중 · 충돌 2` | danger |
  | 분리된 HEAD | `태그 v1.2.0에 있음` | warning |

- 3행: **Fetch / Pull / Push** 버튼. 세 버튼 폭은 같다.
  - Push는 ahead 수를 표시한다 (`Push ↑2`). 추적 브랜치가 없으면
    `게시(Publish)`로 바뀐다. 보낼 것이 없으면 비활성.
  - Pull은 behind가 있으면 FilledButton, 없으면 OutlinedButton.
  - Pull 옆 작은 ▾로 방식(병합 / rebase / fast-forward만) 선택.
- 병합·rebase 중에는 3행이 `[계속] [중단]`으로 바뀐다.
- 명령이 실행되는 동안 헤더 아래에 얇은 진행 표시줄을 보여 준다
  (0.5초 이상일 때만, 규약 §6).

### C. 추천 행동 배너

상태에서 하나를 골라 보여 준다. 우선순위(위가 먼저):

1. 충돌이 있음 → "충돌 파일 2개 해결하기"
2. 원격이 앞섬 → "새 커밋 3개 가져오기 [Pull]"
3. 커밋 안 한 변경 → (배너 없음, 변경 탭 배지로 충분)
4. 원격에 없는 브랜치 → "이 브랜치를 GitHub에 게시 [Publish]"
5. push 안 한 커밋 → "커밋 2개 올리기 [Push]"
6. push했고 PR이 없음 → "Pull Request 만들기"
7. 원격이 없음 → "GitHub에 올리기"

✕로 닫으면 상태가 바뀔 때까지 다시 보이지 않는다.

### D. 섹션 탭

| # | 탭 | 아이콘 (Material Symbols Rounded) | 배지 |
|---|---|---|---|
| 1 | 변경 | `edit_note` | 변경 파일 수 |
| 2 | 브랜치 | `call_split` | — |
| 3 | 태그 | `sell` | 원격에 없는 태그 수 |
| 4 | 원격 | `cloud` | — |
| 5 | 릴리스 | `rocket_launch` | 진행 중인 CI (점) |
| 6 | PR | `merge` | 현재 브랜치 PR 상태 (점) |
| 7 | CI | `bolt` | 실패 (빨간 점) |

- 아이콘 위, 이름 아래(11px). ⌘1–⌘7로 이동. 마지막 탭을 저장소별로 기억한다.
- GitHub 원격이 없거나 `gh` 로그인이 안 됐으면 PR·CI 탭은 비활성과 함께 이유를 툴팁으로.
- **GitHub가 아닌 저장소** (GitLab 등, [PLAN.md](PLAN.md) §2.1):
  - 앱 바 저장소 이름 옆에 호스팅 pill(`GitLab`, `Bitbucket`, `기타 git 서버`,
    textMid)을 붙여 처음부터 알 수 있게 한다. GitHub면 pill을 붙이지 않는다.
  - PR·CI 탭은 비활성. 누르면 빈 상태 화면: 48px `link_off` 아이콘,
    "이 저장소의 원격은 GitLab입니다", "Pull Request와 Actions는 GitHub
    저장소에서만 쓸 수 있습니다 — `gh`는 GitHub 전용 도구입니다." 버튼 없음.
  - 저장소를 처음 열 때 한 번만 안내 배너: "GitLab 저장소입니다. 브랜치·태그·
    병합·Pull·Push는 모두 쓸 수 있고, PR·릴리스·Actions는 쓸 수 없습니다."

### E. 내용 영역 — 공통 패턴

- 섹션 머리: 제목(16 bold) + 주 행동 버튼 1개 + 검색 필드(⌘F).
- 목록 행 높이 36, `NoSplash`, 키보드로 위아래 이동, Enter = 기본 동작,
  우클릭·`⋯` = 메뉴(항목 옆에 단축키 표시).
- 파일 이름, 브랜치 이름, 커밋 메시지는 사용자 콘텐츠이므로 시스템 글꼴
  (conventions `fonts.md` §3). 해시·명령·URL은 고정폭 글꼴(`fonts.md` §4).
- 그룹(예: 로컬/원격 브랜치)은 접을 수 있는 머리와 개수를 붙인다. 접힘
  상태는 저장소별로 기억한다.

### F. 명령 바와 로그 (이 앱 전용 컴포넌트)

- 접힌 상태(높이 32): 상태 아이콘(실행 중 / ✓ / ✕) + 마지막 명령 + 걸린 시간.
- 누르거나 ⌘J → 아래에서 펼쳐지는 로그 패널(창 높이의 최대 50%, 경계를
  끌어서 조절). 명령마다 접을 수 있는 출력, 복사 버튼, 실패는 빨간 테두리.
- 오래 걸리는 명령(clone, push, pull)은 실행 중에 출력이 흘러가고 **취소**
  버튼이 붙는다.
- 실패하면 규약대로 `showErrorSnackBar`(복사 액션)를 띄우고, 자주 나오는
  오류는 쉬운 설명과 해결 버튼을 붙인다. 예: push 거부 → "원격에 내게 없는
  커밋이 있습니다 [Pull 후 다시 Push]".

## 4. 섹션별 화면

### 4.1 변경

```
┌ 커밋 ────────────────────────────────┐
│ [feat][fix][chore][docs] ▾           │  접두어 칩
│ ┌──────────────────────────────────┐ │
│ │ 커밋 메시지                      │ │  여러 줄, 첫 줄 72자 넘으면 경고
│ └──────────────────────────────────┘ │
│ [ 커밋 (스테이징 2) ⌘⏎ ]  □ 직전 커밋 고치기 │
└──────────────────────────────────────┘
▾ 충돌 1
  U  lib/main.dart            lib/       [편집기] [해결됨]
▾ 스테이징됨 2                          [모두 해제]
  M  app_theme.dart           lib/theme/
  A  login_page.dart          lib/pages/
▾ 변경됨 1                              [모두 스테이징]
  M  pubspec.yaml
▸ 추적 안 됨 3
```

- 상태 글자 칩 색: M primary, A success, D danger, R accent, U danger(굵게), ? textLow.
- 행에 마우스를 올리면 오른쪽에 `스테이징/해제`, `편집기로 열기` 버튼.
  변경 취소는 메뉴에만 둔다 (실수 방지).
- 스테이징된 것이 없으면 커밋 버튼 대신 "모두 스테이징하고 커밋".

### 4.2 브랜치

- 그룹: **로컬** / **원격** (원격별 하위 그룹). 현재 브랜치가 맨 위에 고정.
- 행: 현재 표시 점 · 이름 · `↑1 ↓0` · 마지막 커밋 시간(상대 시간).
  원격에서 사라진 브랜치는 이름에 취소선과 "원격 삭제됨" pill.
- 더블클릭/Enter = 전환. 변경이 있어 전환이 막히면 시트로
  "임시 저장(stash)하고 전환 / 변경을 가지고 전환 / 취소".
- 메뉴: 전환 · **현재 브랜치로 병합해 오기** · 게시/Push · PR 만들기 ·
  이름 바꾸기 · 추적 브랜치 설정 · 삭제 · 원격에서 삭제.
- 주 행동: `새 브랜치 ⌘B`.

### 4.3 태그

- 행: 이름(고정폭) · 원격 상태 아이콘(`cloud_done` 올라감 / `cloud_off`
  로컬만) · 날짜 · 메시지 한 줄. 릴리스가 있으면 `rocket` 작은 아이콘.
- 정렬: 버전 순(SemVer, 내림차순). 프리릴리스는 textMid.
- 주 행동: `새 태그 ⌘T`, 로컬에만 있는 태그가 있으면 `모두 Push (2)`.

### 4.4 원격

- 원격마다 카드(`GlassCard`): 이름, fetch/push URL(고정폭, 복사), GitHub면
  `owner/repo` 링크와 공개/비공개 pill, `Fetch` 버튼, `⋯` 메뉴.
- GitHub가 아닌 원격의 카드에는 호스팅 pill과 "GitHub 전용 기능 사용 불가"
  한 줄을 붙인다. 이때 `GitHub에 올리기`는 "GitHub에도 올리기(원격 추가)"로
  바뀐다.
- 원격이 없으면 빈 상태: 48px `cloud_upload` 아이콘, "이 저장소는 아직
  GitHub에 없습니다", `GitHub에 올리기` 버튼.

### 4.5 릴리스 — 마법사 (이 앱 전용 화면)

릴리스 탭 맨 위에 최신 릴리스 요약과 `새 릴리스 ⌘⇧R` 버튼, 아래에 릴리스
목록(P1). 버튼을 누르면 내용 영역이 **세로 단계 화면**으로 바뀐다.
세로 창에 맞춰 Material `Stepper(type: vertical)` 형태로 만든다.

PR 경유가 기본이다 ([PLAN.md](PLAN.md) 3.8.3). 단계는 7개.

```
새 릴리스 v0.4.0                    ✕
① 점검          ✓ 모두 통과 · 수동 빌드 ✓
② 버전          ✓ 0.3.1+7 → 0.4.0+8 (minor) · release/v0.4.0
③ PR            ✓ #12 chore(release): v0.4.0
④ 병합          ● PR #12 병합을 기다리는 중
   ┌──────────────────────────────────┐
   │ 검사 ✓ 0/0 · 리뷰 필요 없음      │
   └──────────────────────────────────┘
   실행될 명령
   ┌──────────────────────────────────┐
   │ gh pr merge 12 --merge           │ 📋
   │   --delete-branch                │
   │ git switch main                  │
   │ git pull --ff-only               │
   └──────────────────────────────────┘
        [ GitHub에서 보기 ] [ 병합하기 ]
⑤ 태그          ○
⑥ 릴리스 노트   ○
⑦ CI 확인       ○
```

- 끝난 단계는 한 줄 요약으로 접히고, 누르면 다시 펼쳐 볼 수 있다
  (이미 실행한 단계는 읽기 전용).
- **① 점검**: 항목마다 ✓/✕와 해결 버튼. 워크플로·플러그인·Flutter 버전·
  플랫폼 설정이 바뀌었으면 아래에 "태그 전 수동 빌드" 카드를 펼쳐 둔다
  (`빌드만 확인` 버튼, 결과는 ⑦과 같은 잡 목록). 건너뛸 수 있다.
- **② 버전**: 추천 버전을 카드 3장(patch / minor / major)으로 보여 주고
  추천에 "추천" pill. 근거("feat 커밋 3개")를 카드 아래에. 바뀌는 파일
  diff를 접힌 상태로 제공. 버전 올림 커밋은 새 브랜치 `release/vX.Y.Z`에
  만든다 (브랜치 이름은 카드 아래에 표시).
- **③ PR**: 제목 `chore(release): vX.Y.Z`, 본문은 이전 태그 이후 커밋 목록
  (편집 가능). `push하고 PR 만들기` 한 버튼.
- **④ 병합**: PR 카드(검사 상태 · 리뷰 · 병합 가능 여부)와 `병합하기`.
  병합할 수 없으면(검사 실패, 리뷰 필요, 권한 없음) 버튼 대신 이유와
  `GitHub에서 보기`를 두고 "병합을 기다리는 중" 상태로 둔다. 창이
  활성화될 때와 주기적으로 PR 상태를 확인해, 병합되면 알아서 `main`으로
  전환·Pull하고 ⑤로 넘어간다.
- **⑤ 태그**: 위에 **태그 전 점검** 체크리스트를 둔다 — PR 병합됨 · `main`이
  원격과 같음 · pubspec 버전 = 태그 · 같은 이름의 태그 없음. 하나라도 ✕면
  태그 버튼이 꺼지고 이유를 보여 준다 (예: "버전 올림 PR이 아직 병합되지
  않았습니다 — main의 버전은 0.3.1입니다"). 모두 ✓면 `태그 달고 push`.
  확인 대화상자에 태그 이름과 대상 커밋을 적고 "이 태그를 push하면 릴리스가
  공개됩니다"를 알린다 (error 색이 아니라 primary — 되돌릴 수 없지만 위험한
  작업은 아니다).
- **⑥ 릴리스 노트**: 탭 `편집 | 미리 보기`. 초안 출처 선택(커밋 목록 /
  GitHub 자동 생성). 편집 영역은 창 높이에 맞춰 늘어난다.
  "CI가 릴리스를 만드는 저장소"면 단계 이름이 `노트 (CI 릴리스 후 적용)`로
  바뀌고, ⑦이 끝난 뒤에 적용된다.
- **⑦ CI 확인**: 잡마다 한 줄(`check ✓ 1m33s`, `build-macos ✕ …`),
  실패한 잡은 펼쳐서 실패 단계와 로그 끝 20줄, `실패한 잡 다시 실행`,
  `브라우저에서 보기`. 기다리는 동안 다른 탭으로 가도 릴리스 탭 배지로
  진행을 알린다. 완료되면 OS 알림(P1). 성공하면 릴리스 링크와 산출물 목록.
- **바로 커밋 방식**(P1, 혼자 쓰는 저장소에서 사용자가 고를 때): ③·④를
  건너뛰고 ②의 커밋을 기본 브랜치에 만든다. 보호된 브랜치면 이 선택지를
  끄고 이유를 보여 준다.
- GitHub가 아닌 저장소에서는 PR을 만들 수 없으므로 바로 커밋 방식으로
  ①·②·⑤(점검, 버전, 태그)만 진행하고, ③·④·⑥·⑦은 회색으로 "GitHub
  저장소에서만"을 붙인 채 건너뛴다. 마지막에 "릴리스 노트는 호스팅 웹에서
  작성하세요"와 태그 이름 복사 버튼을 보여 준다.
- 마법사 도중 창을 닫거나 다른 저장소로 가면 어디까지 했는지 확인 후 떠난다.
  다시 열면 진행 중인 릴리스(열린 릴리스 PR, 진행 중인 CI)를 찾아 그 단계부터
  이어서 보여 준다. 태그를 만들었지만 push하지 않았다면 그 사실을 경고한다.

### 4.6 PR / CI

- PR 탭 위쪽: **현재 브랜치의 PR 카드** (없으면 `PR 만들기`). 카드에 검사
  상태 줄, 리뷰 상태, `병합` 버튼(방식 ▾). 릴리스 마법사 ④와 같은 카드를
  쓴다. 아래: 열린 PR 목록(P1).
- `P2` 카드 아래 **리뷰와 코멘트**: 최근 3건(승인 ✓ · 변경 요청 · 리뷰 ·
  코멘트 아이콘, 작성자 · 시간, 본문 3줄)과 `모두 보기`. 그 아래 `승인` ·
  `변경 요청` · `코멘트` 버튼 → 시트(Markdown 본문 + 실행될 명령). 변경 요청과
  코멘트는 본문이 있어야 보낼 수 있다. 내 PR이면 `코멘트`만 보이고 이유를
  한 줄로 알린다. 릴리스 마법사 ④의 카드에는 넣지 않는다.
- `P2` 릴리스 노트 시트 위쪽에 **첨부 파일**: 이름 · 크기 · 받은 횟수, 행마다
  `받기`(폴더 고르기) · `삭제`(확인), 머리에 `파일 올리기`(여러 개). 같은 이름을
  올리면 덮어쓰기 확인을 한 번 더 받는다.
- CI 탭: 현재 브랜치의 최근 실행 목록, 행 = 워크플로 이름 · 상태 pill ·
  트리거 · 시간.

## 5. 양식과 확인

- **양식은 아래에서 올라오는 시트**로 연다 (가운데 대화상자 대신). 좁은
  세로 창에서 폭을 모두 쓰고, 편집기 쪽을 가리지 않는다. 최대 높이 85%,
  모서리 `AppRadius.sheet`.
- 모든 실행 양식은 버튼 바로 위에 **"실행될 명령"** 블록(고정폭, 복사
  버튼)을 둔다. 입력을 바꾸면 즉시 갱신된다.
- 확인 대화상자는 규약 §6 그대로(되돌릴 수 없는 작업만, 위험 버튼은 error
  색, 기본 포커스는 취소). 이 앱에서 확인이 필요한 작업: 원격 브랜치 삭제,
  원격 태그 삭제, 강제 push, 변경 취소, 병합 중단, 릴리스 삭제, 원격 삭제.
- 원격을 바꾸는 작업(push, 원격 삭제)의 대화상자에는 대상
  `origin → github.com/owner/repo`를 적는다.

## 6. 용어와 문구

- 버튼에는 **git 용어를 그대로** 쓴다(Push, Pull, Fetch, 브랜치, 태그,
  병합). 사용자가 다른 곳에서 만날 말이기 때문이다. 대신 툴팁과 `?`
  도움말에 한 문장 설명을 붙인다. 예: Pull — "GitHub에 있는 새 커밋을 내
  컴퓨터로 가져와 합칩니다."
- 숫자에는 단위를 붙인다: "커밋 2개", "파일 3개".

### 6.1 개념 도움말 표시 ([PLAN.md](PLAN.md) 3.13)

- **여는 곳**: 옵션 이름 옆 `?` 아이콘(16px, textLow), 오류 스낵바의
  "왜 그런가요?" 액션, F1(현재 화면과 관련된 용어 목록).
- **카드**: 아래에서 올라오는 시트(규약 §5 양식과 같은 모양). 세로 창에
  맞춰 위에서 아래로 `낱말 뜻 → git에서는 → 왜·언제 → 그림 → 더 알아보기`.
  - "낱말 뜻"과 "git에서는" 사이에 `↓` 연결 표시를 두어 두 뜻이 이어진다는
    것을 눈으로 보여 준다.
  - 그림은 `CustomPaint`로 그린 커밋 그래프: 점(커밋) · 선 · 브랜치 이름표
    pill. 실행 전/후 두 칸을 위아래로 두고, 달라진 부분(옮겨진 이름표, 새
    커밋)만 primary 색. 라이트/다크 모두 테마 토큰으로.
  - 링크는 맨 아래 `open_in_new` 아이콘과 함께, 외부 브라우저로 연다.
- **비교 화면** (병합·Pull 방식): 방식마다 한 행 = 이름 + 결과 그래프 축소판 +
  "이럴 때" 한 줄. 지금 쓸 수 없는 방식은 흐리게 하고 이유를 표시한다.
  비교 화면에서 방식을 누르면 그 방식이 선택된 채로 양식으로 돌아간다.
- **처음 한 번 안내**: 병합 방식이나 Pull 방식을 처음 고를 때만 옵션 아래에
  "처음이세요? 차이 보기" 링크를 강조한다. 한 번 열면 다시 강조하지 않는다.
- 오류 문구는 "무엇이 일어났나 → 왜 → 무엇을 하면 되나" 순서.

## 7. 추가 단축키 (규약 §7 표 외)

| 기능 | 키 |
|---|---|
| 섹션 이동 | ⌘1 – ⌘7 |
| Push | ⌘P |
| Pull | ⌘⇧P |
| Fetch | ⌘⇧F |
| 커밋 | ⌘⏎ (메시지 입력 중) |
| 새 브랜치 | ⌘B |
| 새 태그 | ⌘T |
| 새 릴리스 | ⌘⇧R |
| 편집기로 열기 (선택한 파일) | ⌘E |
| 명령 로그 펼치기/접기 | ⌘J |
| 항상 위에 표시 | ⌥⌘T |

- macOS 메뉴 막대(`PlatformMenuBar`)에 `저장소`(열기, 최근, Fetch/Pull/Push),
  `브랜치`(새 브랜치, 전환…), `릴리스`(새 릴리스) 메뉴를 더한다.

## 8. 상태 화면

- **빈 상태** (저장소 없음): 규약의 빈 상태(48px 아이콘, 한 줄 안내,
  `저장소 열기` 버튼) 아래에 **최근 저장소 목록**을 덧붙인다. 창 전체가
  폴더 드롭 영역이다.
- **환경 점검 실패**: `git`/`gh`가 없거나 로그인이 안 됐을 때 항목별
  체크리스트(✓/✕)와 설치 명령(`brew install gh` 등, 복사 버튼),
  `다시 확인` 버튼. `gh`가 없어도 git 기능(변경~원격)은 쓸 수 있고, PR·CI·
  릴리스만 막는다.
- **로그인 안내** (`gh` 로그인이 안 됐을 때, [PLAN.md](PLAN.md) 3.1):
  - 맨 위에 방식 선택 `SSH | HTTPS`. 원격 URL이 SSH 형식이거나 SSH 세션에서
    실행 중이면 SSH를 기본으로 고르고 "SSH 권장" pill과 이유 한 줄을 붙인다.
  - SSH를 고르면 5단계 체크리스트(키 확인 → 로그인 → 기기 코드 → 확인 →
    HTTPS에서 전환)를 세로로 보여 준다. 단계마다 설명 한 줄과 명령
    블록(고정폭, 복사 버튼)을 두고, 앱이 확인할 수 있는 단계(키 파일 있음,
    `gh auth status`의 프로토콜, `ssh -T` 결과)는 ✓/✕로 자동 표시한다.
  - 원격 서버(브라우저 없음)면 3단계를 펼쳐 두고
    https://github.com/login/device 링크와 코드 입력 방법을 강조한다.
  - 키와 암호 문구는 앱에서 입력받지 않는다. 명령을 복사해 터미널에서
    실행하도록 안내하고, 끝나면 `다시 확인`을 누르게 한다.
- **저장소가 아님**: "이 폴더는 git 저장소가 아닙니다" + `git 저장소로
  만들기`(P1) / `다른 폴더 열기`.
